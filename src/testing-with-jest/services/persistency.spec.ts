describe("TESTANDO ALGUMA COISA (it)", () => {
     it("descricao do teste (it)", () => {
          const number = 1
          expect(number).toBe(1)
     })
})

describe("TESTANDO ALGUMA COISA (test)", () => {
     test("descricao do teste (test)", () => {
          const nome = "thiago"
          expect(nome).toBe("thiago")
     })
})
